{$DOMAIN} {
	encode zstd gzip

	# Proof uploads are capped at 10 MB by `saveUpload`; reject bigger bodies
	# here rather than streaming them into the app first.
	request_body {
		max_size 12MB
	}

	reverse_proxy app:3000
}
