# Everything the build gets from `npm ci` / `next build` rather than the host.
node_modules
.next
out
build
src/generated

# Local state that must never leak into an image layer.
.env
.env.*
!.env.example
**/*.db
**/*.db-journal
**/*.db-wal
**/*.db-shm
backups
uploads
test-results
playwright-report
tests/.uploads
e2e/.uploads
e2e/.world.json

# Not part of the runtime.
.git
.github
.claude
docs
e2e
tests
playwright.config.ts
vitest.config.ts
README.md
CLAUDE.md
Dockerfile
compose.yaml
Caddyfile
