# Local Claude Code settings (machine/dev, not shared)
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db

# Editors
.idea/
.vscode/
*.swp

# Add stack-specific build/dep artifacts here (node_modules/, dist/,
# .venv/, target/, ...) at bootstrap time.
